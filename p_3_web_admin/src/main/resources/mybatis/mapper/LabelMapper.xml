<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ppppp.mapper.LabelMapper">

<!--    public Label getLabel(Long labelId); -->
    <select id="getLabel" resultType="ppppp.bean.Label">
        select * from  t_label where  labelId=#{labelId}
    </select>



</mapper>